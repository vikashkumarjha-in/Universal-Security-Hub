# Nmap Execution Module
This directory acts as the localized development boundary for structural network recon logic. 
The core engine binds standard Nmap execution arguments synchronously back into the asynchronous parsing threads of `main_gui.py`.